<h1>View Patient</h1>

<%= render @task %>

<%= link_to "Edit Patient", edit_task_path(@task) %> |
<%= link_to "Return to Patients", tasks_path %>
<%= button_to "Delete Patient", task_path(@task), method: :delete, class:"btn btn-danger mt-3", form: { data: {turbo_confirm: "Are you sure?" } } %>

