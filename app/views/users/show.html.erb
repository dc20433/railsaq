<h1>View User</h1>

<%= render 'user' %>

<%= link_to "Edit User", edit_user_path(@user) %> |
<%= link_to "Return to Users", users_path %>
<%= button_to "Delete User", user_path(@user), method: :delete, class:"btn btn-danger mt-3", form: { data: {turbo_confirm: "Are you sure?" } } %>