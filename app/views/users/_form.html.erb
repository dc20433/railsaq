<%= bootstrap_form_for(@user) do |f| %>
  <%= f.alert_message "Please fix the errors below.", error_summary: false %>
  <%= f.text_field :name %>
  <%= f.text_field :email %>
  <%= f.primary @user.persisted? ? "Update User" : "Create User" %>
<% end %>